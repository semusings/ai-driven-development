# Quarkus HTTP server port
quarkus.http.port=8082

# Application name (optional, for reference)
quarkus.application.name=tweet-service

# Enable health endpoints (default: enabled)
# Health endpoints: /q/health, /q/health/live, /q/health/ready
